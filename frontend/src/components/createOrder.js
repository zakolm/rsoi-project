import React from "react";
import {Navigate} from "react-router-dom";

export const CreateOrder = (email) => {
    console.log('======>');

    return (
        <>
            <Navigate to="/"/>
        </>
    );
}
